boolean[] keys = new boolean[255]; //saber que keys estão a ser primidas (isto premite saber se estão a ser primidas ao mesmo tempo)

float vox = 10; //unidade base do projeto

float renderDistance = 100 * vox;